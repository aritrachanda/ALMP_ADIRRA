duckdb>=0.10.0
pyyaml>=6.0
google-genai>=1.0.0
openai>=1.30.0
python-dotenv>=1.0.0
pandas>=2.0.0
openpyxl>=3.1.0
faiss-cpu>=1.7.0
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
pydantic>=2.0,<3.0
sse-starlette>=2.0.0,<3.0
httpx>=0.27.0,<1.0
python-multipart>=0.0.9

# Governance database (Business Glossary v2 — first slice of YAML/DuckDB -> PostgreSQL).
# psycopg[binary] ships a cp314 Windows wheel (verified 2026-07-23), so no build toolchain
# is needed on this venv's Python 3.14. SQLAlchemy is already present transitively; pinned
# explicitly here now that it is a direct dependency.
SQLAlchemy>=2.0,<3.0
psycopg[binary]>=3.2,<4.0
alembic>=1.13,<2.0

# Testing
pytest>=8.0.0
