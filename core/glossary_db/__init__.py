"""Business Glossary v2 Postgres data-access layer (Phase 2).

Dormant until project.yaml `database.glossary_backend` (or ADIRRA_GLOSSARY_BACKEND) is set to
'postgres' — the default 'yaml' keeps the running app on the legacy store until Phase 3
migrates the data.
"""
